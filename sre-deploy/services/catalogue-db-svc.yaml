apiVersion: v1
kind: Service
metadata:
  name: catalogue-db
  namespace: sock-shop
spec:
  selector:
    app: mysql    # or whatever label your MySQL pod has
  ports:
    - port: 3306
      targetPort: 3306
  type: ClusterIP
